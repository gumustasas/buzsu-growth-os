# Airtable Connector — Workflow Notları

Bu klasör Airtable bağlantısının mimari yer tutucusudur (`.gitkeep`).

## Kural (CLAUDE.md ile birebir)

- Bu repodan Airtable'a yalnızca **okuma** yapılır; yazma yapılmaz.
- Müşteri telefon/isim bilgisi `outputs/` veya `drafts/` dosyalarına yazılmaz.

İlgili skill: `skills/mcp/` (Airtable MCP server kullanım kuralları)
